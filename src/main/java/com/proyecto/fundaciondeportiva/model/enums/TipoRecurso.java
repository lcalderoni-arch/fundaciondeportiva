package com.proyecto.fundaciondeportiva.model.enums;

/**
 * Define los tipos de recursos que un profesor puede subir.
 * Basado en: ENUM('ARCHIVO','VIDEO','LINK','IMAGEN','PDF','DOCUMENTO','OTRO')
 */
public enum TipoRecurso {
    ARCHIVO,
    VIDEO,
    LINK,
    IMAGEN,
    PDF,
    DOCUMENTO,
    OTRO
}