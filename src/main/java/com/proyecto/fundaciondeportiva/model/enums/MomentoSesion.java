package com.proyecto.fundaciondeportiva.model.enums;

/**
 * Define cuándo se utiliza un recurso en una sesión.
 * Basado en: ENUM('ANTES','DURANTE','DESPUES')
 */
public enum MomentoSesion {
    ANTES,
    DURANTE,
    DESPUES
}